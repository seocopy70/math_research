# Phase 2-10 — W versus Lambda^2(Sym^2 V)

## Question
Test explicitly whether the 45-dimensional module W obtained from the orbit of T is isomorphic to the natural 45-dimensional module Lambda^2(Sym^2(V)) for V the natural 4-dimensional Sp4(F3)-module.

## Why this is the correct next test
The computation already proves dim(W)=45 and U=im(N) ~= Sym^2(V), while M=ker(N)/im(N) is 25-dimensional and irreducible. Cabanes–Marin record that Lambda^2(Sym^2(V)) has composition factors Sym^2(V) twice and the 25-dimensional irreducible once. This is structural evidence only; Phase 2-10 tests actual generator-matrix isomorphism.

## Certificate sought
For the same five generators g_i, construct A_i^model on Lambda^2(Sym^2(V)) and solve

P A_i^W = A_i^model P.

A rank-45 solution P proves W ~= Lambda^2(Sym^2(V)).

No identification is assumed from dimension or composition factors alone.

## Current status
Script added to GitHub and a dedicated Actions workflow added. Execution result must be checked before claiming success or failure.
