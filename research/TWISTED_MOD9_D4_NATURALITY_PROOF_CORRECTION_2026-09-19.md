# TWISTED MOD-9 — D4 TRUNCATION AND PRESENTATION-CHANGE NATURALITY
## Proof-level correction and closure attempt — 2026-09-19

### 1. D4 truncation: exact mechanism

Let A2=Z/9 with action rho(x)=1+3 lambda(x) and let f:F->F3 be the reduction of a lifted crossed homomorphism. Write, modulo 9,

z_{lambda,f}(w)=z_{0,f}(w)+3 B_{lambda,f}(w).

Here z_{0,f}(w) is the ordinary additive/exponent-sum term and B_{lambda,f} is the first-order coefficient-action correction. From the crossed-word rule, B is a bilinear Magnus functional of filtered degree at most 2: it is obtained by pairing one letter evaluation by f with one prefix augmentation evaluation by lambda. Equivalently, it factors through the degree-2 truncation of the augmentation filtration.

For the ordinary term, if d in D4(F), then the Zassenhaus product formula for p=3 gives

D4 = gamma_4(F) · gamma_2(F)^3 · gamma_1(F)^9.

Every factor has abelianization divisible by 9: gamma_4 and gamma_2 have zero abelianization, while ninth powers contribute multiples of 9. Hence

z_{0,f}(d)/3 = 0 in F3.

For the deformation term, B_{lambda,f} kills D3, hence in particular D4. One can see this directly on the generators of D3=gamma_3(F) F^3: degree-2 Magnus coefficients vanish on gamma_3, and the cubic power correction on x^3 is proportional to 0+1+2=3, hence vanishes modulo 3. Therefore

B_{lambda,f}(d)=0,

and consequently

z_{lambda,f}(d)/3 = 0 in F3.

Thus the relator obstruction modulo 9 factors through the relation jet modulo D4. This is the missing publication-level step behind the earlier informal “D4 errors disappear” sentence.

### 2. Degree-3 bracket versus power projection

For the trivial-action part z_{0,f}, a degree-3 Lie bracket has zero abelianization. Hence the degree-3 contribution can survive only through the restricted-power component P -> p(P) in V^(1). The Frobenius identification V^(1) ~= V over F3 is canonical in the present finite-dimensional setting.

Therefore

 delta_rho(f) = [ f(p(P)) + (lambda wedge f)(R) ] omega.

This establishes the required truncation formula, subject only to the standard transgression identification of the one-relator obstruction with H2.

### 3. Presentation-change lemma: algebraic core

Let alpha:F->F be a free automorphism inducing the identity on G. Minimality implies

 alpha(x_i)x_i^{-1} in ker(F->G)=R.

Since the initial relation ideal is generated by the single quadratic class R2, the degree-2 part of the generator change has the form

 X_i -> X_i + c_i R2 modulo degree >=3.

Substituting this into R2 changes it only in degree 3:

 alpha(R2)=R2+[v,R2] modulo L_ge4,

where v=c_1 X_2-c_2 X_1+c_3 X_4-c_4 X_3 for the frozen symplectic convention. The degree-3 component P is unchanged by this substitution at degree 3, because replacing a degree-1 variable by a degree-2 correction inside a degree-3 expression raises degree to at least 4.

Separately, changing the generator of the one-dimensional relation-module class multiplies the entire relation jet by a unit u. Relator conjugation contributes the standard degree-3 gauge [v,R2]. Thus, PROVIDED the standard one-relator relation-module change theorem is invoked to identify the kernel action with unit scaling plus conjugation, the degree-(2,3) transformation is

 (R2,P3) -> (u R2, u P3 + [v,R2]).

Since f([v,R2])=0 for every degree-one functional f, the recovery functional transforms by

 Theta' = u Theta,

so its zero set is invariant.

### 4. What is and is not closed

The D4 estimate above supplies the missing explicit filtration mechanism; it should replace the earlier one-line exponent-sum argument in the manuscript.

The presentation-change argument is now reduced to one external/standard structural input: the precise relation-module statement that an automorphism of a minimal one-relator pro-3 presentation over id_G changes the defining relation class by a unit and conjugation, with no additional degree-3 gauge after the degree-2 generator correction is accounted for. The repository's current internal calculations do not by themselves prove that theorem.

Therefore the strict status is:

- D4 truncation: **PASS / CLOSED**, modulo the standard Zassenhaus product formula and the explicit crossed-word/Magnus factorization stated above.
- Presentation-change naturality: **CONDITIONAL / OPEN**, pending the exact standard cover-change theorem or a self-contained proof.
- Frozen-coordinate twisted recovery: **PASS / CLOSED**.
- Projective carrier sufficiency: **PASS / CLOSED in the frozen degree-(2,3) model**.
- Absolute categorical minimality: **OPEN**.

### 5. Next step

Verify the remaining cover-change input against primary literature or derive it from the one-relator relation-module sequence. If it is confirmed, the intrinsic mod-9 theorem can be closed. If not, the exact residual gauge must be added to the carrier category.
