# HA61-B5-3 — COMBINED AFFINE SECONDARY QUOTIENT — 2026-09-20

## Status
**PASS / LOCAL for the affine gauge quotient; HA61-B remains OPEN / LOAD-BEARING.**

The B5-2 calculation can be sharpened into an explicit combined affine action. The raw vector (t_2) is not intrinsic, but its gauge shift is exactly paired with the compensating shift of the secondary coefficient character (mu).

## 1. Input
On the primary-zero locus,
[
delta_2(f)=igl[f(p)+(lambdawedge f)(R)igr]omega=0,
]
so ( (lambdawedge f)(R)=-f(p)).

The secondary obstruction has the audited form
[
delta_3(f)=igl[f(t_2)+(muwedge f)(R)igr]omega.
]

A cubic relator-gauge change (P_3mapsto P_3+[v,R]) gives
[
t_2mapsto t_2+a,p,qquad a=lambda(v),
]
up to the fixed global sign convention.

## 2. Forced compensator
Under (t_2mapsto t_2+a p), the first term changes by (a f(p)). If simultaneously
[
mumapstomu+alambda,
]
the second term changes by
[
a(lambdawedge f)(R)=-a f(p).
]
Hence the two changes cancel and (delta_3(f)) is unchanged.

The compensator is forced by the primary obstruction identity; it is not an arbitrary gauge choice.

## 3. Intrinsic secondary object
The natural gauge action is
[
(t_2,mu)mapsto(t_2+a p,mu+alambda),qquad ainoperatorname{im}lambda.
]
For nonzero (lambda), (operatorname{im}lambda=mathbf F_3), so the raw pair is replaced by
[
oxed{[(t_2,mu)]in (V^{(2)}oplus V^*)/mathbf F_3(p,lambda).}
]
Equivalently, it is an affine torsor for the diagonal line generated by ((p,lambda)). The obstruction functional descends to this quotient on the primary-zero domain. If (lambda=0), this particular affine ambiguity disappears.

## 4. What is proved
- raw (t_2) is not presentation-free in general;
- the cubic bracket sector does not introduce a second independent secondary functional;
- the correct object is a combined affine quotient/torsor, not (V/mathbf F_3p) alone;
- the compensating (mu)-shift is forced by the primary obstruction;
- the secondary obstruction is a functional of the combined quotient class.

## 5. Remaining load-bearing gaps
This does not yet prove universal B5 closure. We still need:
1. the full allowed relator-gauge group and proof that it induces no additional independent affine directions;
2. vanishing/factorization of every (E_{>P_4}) contribution through the same quotient;
3. functoriality under filtered isomorphisms and independence of the remaining (H^2)-generator/common-scaling convention.

Therefore HA61-C remains unopened.

## 6. Decision
- combined affine action: **PASS / LOCAL**;
- raw (t_2) intrinsicity: **FAIL / CLOSED**;
- no independent cubic-bracket functional: **PASS / LOCAL**;
- universal affine secondary carrier: **OPEN / LOAD-BEARING**;
- deeper-than-(P_4) factorization: **OPEN / LOAD-BEARING**;
- HA61-B overall: **OPEN / LOAD-BEARING**;
- HA61-C: **not opened**.

## Next authorized attack
Complete the source audit at the quotient level: identify the full relator-gauge image, prove its only surviving affine direction is ((p,lambda)), then attack (E_{>P_4}) generators and test whether their /9 evaluations factor through the same class. Only then open HA61-C.
