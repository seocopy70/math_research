# O2-2 Result — obstruction equivariance and transported action

Date: 2026-09-18

## Status

**O2-2 = PASS / COMPLETE.**

Verified execution:
- GitHub Actions run: `35278644641`
- Job: `105395199074`
- Corrected artifact source: commit `62886877f97e58e87d59b0075d45e38be6176410`
- Downstream working commit at verification: `5a447946e9bb7087078e22eccc9bd304a7170494`

## Verified facts

1. **O2-2R — generator-level obstruction equivariance: PASS.**
2. **O2-2S — group consistency of the solved transported action: PASS.**
3. **O2-2T — exact identification of the transported action: PASS.**

For every one of the five generators, the independently solved 16-parameter matrix satisfies exactly
\[
T_g=g^{-T}\quad\text{over }\mathbb F_3.
\]
Generator-pair multiplicativity was also verified, so the solved action is consistent with the generated group.

Consequently the stacked obstruction satisfies
\[
D_{\rm stack}A_W(g)
 = (T_g\otimes A_5(g))D_{\rm stack}
 = (g^{-T}\otimes A_5(g))D_{\rm stack}
\]
for the five generating elements, hence for all \(g\in H=Sp_4(\mathbb F_3)\).
Therefore
\[
O=\operatorname{Im}D_{\rm stack}
\]
is an \(H\)-submodule.

## Convention audit status

The historical question "was a legacy tuple-action implementation explicitly using a transpose/inverse-transpose convention?" remains **NOT YET DETERMINED**.

This does **not** block O2-3. The O2-2 result was obtained by solving for \(T_g\) from the current data rather than assuming a legacy convention, and the exact result \(T_g=g^{-T}\) passed the independent word-collision and group-consistency checks.

## Final pre-O2-3 identity check

The \(A_W(g)\) used for O2-2 is the same mathematical action used to define the nilpotent endomorphism \(N\), not merely a numerically similar representation:

- The authoritative `phase2_3_endH_optimized_2026-09-15.py` loads `action_matrices` from `phase2_1_invariant_space_verification_2026-09-15.py`.
- That Phase 2-1 script constructs the action from `apply_linear_map(a, g)` using the column convention
  \(e_j\mapsto\sum_i g_{ij}e_i\).
- The corrected A3-4 pipeline uses the same `apply_linear_map` definition and the same five `gens` construction.
- The corrected O2-2 script imports that verified pipeline and constructs `A_W` from those same ambient actions.

Thus the representation on \(W\) used in the definition of \(N\) and the representation denoted \(A_W(g)\) in O2-2 are the same action/convention.

## Next gate: O2-3

Proceed to
\[
U=\operatorname{Im}N\n\xrightarrow{\ \bar D_{\rm stack}\ } O=\operatorname{Im}D_{\rm stack}.
\]

Required order:
1. trace/character precheck;
2. verify \(\ker D_{\rm stack}=I_W=\ker N\);
3. verify \(\dim O=10\);
4. construct and verify the induced 10-dimensional map \(\bar D_{\rm stack}:W/I_W\to O\);
5. identify \(W/I_W\cong U\) and test \(H\)-equivariance;
6. only if necessary, solve an explicit 10x10 intertwiner.

No legacy-convention archaeology is required before this gate.
