# Q3/Q∞ zero-case pre-registration — 2026-09-18

## Purpose

Before any q=3 versus q=∞ comparison, fix a single invariant that is defined for both nonzero and zero values of (N(d)), so that the comparison does not require an ad hoc projective zero convention.

## Pre-registered invariant

For (v \in U), define the **vector-orbit invariant**

[
J(v) := |H_U \cdot v|,
]

where (H_U=\operatorname{im}(H\to GL(U))).

This is defined for every (v\in U), including (v=0). In particular,

[
J(0)=1
]

because the zero vector is fixed by every linear transformation.

The previously observed projective invariant

[
I([v])=|H_U\cdot[v]|
]

remains a structural candidate for nonzero vectors, but it is **not** the pre-registered q-comparison invariant until a corresponding vector-orbit audit is completed.

## q=∞ zero case

If the mathematically defined q=∞ construction yields (N(d_\infty)=0), the comparison uses (J(0)=1). No artificial assignment such as “projective orbit size = 0” is permitted.

If a future mathematically justified q=∞ construction yields a nonzero (N(d_\infty)), the same (J(v)) is applied without modification.

## Scope and non-circularity

- The q=∞ object must be defined independently from this invariant.
- No q=∞ value may be set to zero merely to obtain separation.
- No threshold, ranking, or post-hoc exceptional-case rule may be introduced after observing the q=3 result.
- The current size-40 projective orbit is not promoted to a q-detector by this pre-registration.

## Required next gate

Before Q3/Q∞ comparison:

1. give a mathematical definition of (d_\infty);
2. derive (N(d_\infty)) from that definition;
3. compute (J(N(d_3))) and (J(N(d_\infty)));
4. only then assess whether the invariant distinguishes the two cases.

## Current status

The size-40 projective orbit is a **candidate invariant observed stably inside (H\cdot d_3)**. Its dependence on the present local model and H-action remains an open possibility.
