# O2-7 Result — q-control of the transport-variation module

Date: 2026-09-18
Run: 35284838948
Final code commit: 988f61c04f3c659db2b4bff285f8f0809d4e7f4f

## Verdict

**O2-7 = INFORMATIVE FAIL for the original identification hypothesis.**

The raw q=3 p-power class d=[X1^[3],X2] does not generate the rank-10 module U. Its H-orbit generates all of W.

Verified log:
- rank(d_q3)=1 in Q4.
- rank(<H.d_q3>)=45.
- rank(U=im(N))=10.
- rank(N(d_q3))=1.
- rank(<H.N(d_q3)>)=9.
- rank(U + <H.N(d_q3)>)=10.
- rank(im(tau N))=10.
- im(tau U) = im(tau N) exactly at the tested subspace level.
- q=infinity p-power control gives d_infinity=0.

## Interpretation

The first proposed identification was too strong:

    H-orbit of the q-sensitive class d  ==  U

is false.

In fact d is a cyclic vector for the full 45-dimensional W. Applying N to d produces a nonzero vector whose H-span has dimension 9, sitting inside the 10-dimensional U. Thus the q-sensitive class has a nontrivial but incomplete shadow in U.

This is a useful structural result rather than a dead end: the 10-dimensional variation module is not simply the H-orbit of the visible p-power class.

## What survives

1. A3-4-2 already gives an independent q=3 versus q=infinity separation in Q4 through [X1^[3],X2] != 0.
2. O2-5/O2-6 still establish a controlled 10-dimensional transport-variation module.
3. O2-7 shows that the naive direct identification of those two objects is wrong.
4. The new 9-dimensional H-submodule <H.N(d)> inside U is a concrete object worth auditing next.

## Next checkpoint

The next experiment should not force U to equal the p-power orbit. Instead test the exact extension position

    <H.N(d)>  subset  U

with dimensions 9 and 10, and characterize the one-dimensional quotient U/<H.N(d)> together with its relation to the transport-variation map Delta D.

The key question becomes whether the missing one-dimensional direction is the actual carrier of the q=3-specific information, rather than the whole 10-dimensional U.

## Boundary

This does not recover the orientation character chi. It also does not show that Delta O is q-intrinsic. The q=infinity control used here only suppresses the p-power contribution; it is a control for this local question, not a complete recomputation of the entire q=infinity filtration.