# HA61-B5-14 — GLOBAL ZERO-MAP QUANTIFIER CORRECTION — 2026-09-20

## Status

**PASS / LOCAL for the corrected logical implication; OPEN / DECISIVE for the universal variation identity and finite filtered existence/factorization.**

This record corrects the quantifier error in the first B5-13 uniqueness attack. The previous fixed-(f) analysis is mathematically valid for that different question, but it is not the research selector problem.

## 1. Fixed-(f) versus global zero-map

For a fixed (f),
[
Z_3(f)={ho_3:delta_{3,ho_3}(f)=0}
]
may have an affine kernel of dimension (3) if the variation formula is
[
delta_{3,ho_3(1+9
u)}(f)-delta_{3,ho_3}(f)=pm(
usmile f).
]
Thus the fixed-(f) zero set can have (3^3=27) points.

However, the intended selector asks for a coefficient lift satisfying the **entire connecting map**
[
oxed{delta_{3,ho_3}equiv0}
]
on all of (H^1(G,mathbf Z/9(ho_2))), not merely for one chosen (f).

## 2. Correct uniqueness implication

Assume:

1. the universal variation identity holds for every (f);
2. two lifts satisfy (delta_{3,ho_3}=0=delta_{3,ho_3'});
3. (ho_3'=ho_3(1+9
u));
4. the reduction
[
H^1(G,mathbf Z/9(ho_2))longrightarrow H^1(G,mathbf F_3)
]
is surjective;
5. the Demuškin mod-3 cup pairing on (H^1(G,mathbf F_3)) is nondegenerate.

Then for every (f) in the mod-9 domain,
[
0=delta_{3,ho_3'}(f)-delta_{3,ho_3}(f)
=pm(
usmile ar f).
]
Surjectivity gives (ar f) ranging over all (H^1(G,mathbf F_3)). Hence
[

usmile v=0qquadorall vin H^1(G,mathbf F_3).
]
Nondegeneracy forces (
u=0), and therefore (ho_3'=ho_3).

So the corrected statement is:

[
oxed{	ext{global zero-map lift is unique, conditionally on (1)--(5).}}
]

This does **not** say that a fixed-(f) zero is unique.

## 3. What remains open

The above does not establish existence. It also does not establish the universal variation identity.

The load-bearing gates remain:

- **universal variation identity:** OPEN / DECISIVE;
- **global zero-map existence:** OPEN / DECISIVE;
- **existence from declared finite filtered/relation input:** OPEN / DECISIVE;
- **factorization through (G/P_4) (equivalently the established (D_{10}) window):** OPEN / DECISIVE;
- **novelty separation from Serre/Kummer's classical coefficient-lifting characterization:** OPEN.

The construction must not import (q), (chi), the dualizing action, or an equivalent canonical orientation object.

## 4. Supersession boundary

The following earlier conclusions are now explicitly superseded:

- B5-13's fixed-(f) kernel argument **must not** be used as a no-go theorem for the global zero-map selector.
- The claim that the full global selector necessarily has 27 candidates because one (f) has a 3-dimensional orthogonal complement is **HISTORICAL / SUPERSEDED**.
- B5-13 remains closed only for the incorrect **fixed-(f) uniqueness mechanism**; it does not close the global zero-map uniqueness implication.

The earlier (t_2), (t_2/langle pangle), and diagonal ((t_2,mu)) carrier routes remain FAIL / CLOSED and are not resurrected.

## 5. Next authorized attack

Attack the surviving global question in this order:

[
oxed{
	ext{universal variation identity}
	o
	ext{global zero-map existence}
	o
	ext{finite filtered factorization}
	o
	ext{Serre/Kummer novelty audit}.
}
]

No (t_2) resurrection and no all-(n) induction.

