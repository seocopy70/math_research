# HA61-B5-11 — FINITE-DEPTH SOURCE LEDGER AT D_4 / MOD-27 — 2026-09-20

## Verdict

**PASS / LOCAL for the finite-depth source classification; HA61-B remains OPEN / LOAD-BEARING.**

The mod-27 / division-by-9 source audit can be completed at the D_4 threshold without any all-n induction.

## 1. Finite-depth valuation facts

Let z be a crossed 1-cocycle for rho_3 with rho_3 ≡ 1 (mod 3), reducing to f modulo 3.

The already established degree-2 and degree-3 commutator formulas imply, at the required finite depth,

- z(gamma_2) is divisible by 3;
- z(gamma_3) is divisible by 9.

Applying the same commutator identity once more to [gamma_3,F] gives z(gamma_4) divisible by 27. This is only the finite chain gamma_2 -> gamma_3 -> gamma_4 needed here; it is NOT an all-n induction.

For w in gamma_2(F), rho_3(w)=1 because rho_3 factors through the abelianization. Hence
z(w^3)=3z(w).
Therefore:
- gamma_2^3 can survive after division by 9;
- gamma_3^3 is divisible by 27 and vanishes after division by 9 mod 3;
- gamma_4 and deeper ordinary commutator terms vanish after division by 9.

## 2. Zassenhaus D_4 source decomposition

At p=3,
D_4 = gamma_4(F) · gamma_3(F)^3 · gamma_2(F)^3 · F^9
(up to the standard product ordering, which is irrelevant for the associated finite quotient).

The preceding valuation calculation therefore shows that, after applying z and dividing by 9 modulo 3, only two source types can survive:

1. **F^9:** for a generator g,
   z(g^9) = (1+rho(g)+...+rho(g)^8)z(g)
   and, for rho(g)=1+3a mod 27,
   1+rho(g)+...+rho(g)^8 ≡ 9 mod 27.
   Hence
   z(g^9)/9 ≡ f(g) mod 3.

2. **gamma_2^3:** the already audited formula gives
   z([g,h]^3)/9 = (lambda wedge f)([g,h]) mod 3.
   This is the old first-stage obstruction sector, not a new independent functional.

No gamma_3^3 or gamma_4 contribution survives.

## 3. Consequence for the B5 residual

The blanket statement “D_4-errors vanish” is false, but the surviving source space is now sharply classified at this finite depth:

E_{>=4}/9 mod 3 is generated by the F^9 power functional and the old gamma_2^3 / (lambda wedge f) sector.

Thus there is no third, previously hidden commutator source at D_4 capable of producing an independent secondary functional.

This is stronger than the earlier qualitative statement “deeper terms may vanish”: the entire D_4 source list has been reduced to the two explicit surviving sectors.

## 4. What this does NOT yet prove

It does not by itself prove that the F^9 residual has a canonical presentation-free representative called t_2.

In particular, the exact relator-conjugation identity from B5-9 says that the isolated [v,R] term is not the complete coordinate transformation. The prefix/suffix terms must remain in the same ledger. Therefore the finite source classification closes the valuation question but not the coordinate/naturality question.

Likewise, the intrinsic family
rho_3 |-> delta_{3,rho_3}
is already canonical, but extracting a single filtered t_2 from that family remains a separate factorization theorem.

## 5. Classification

- D_4 blanket vanishing: **FAIL / CLOSED**.
- D_4 finite-depth source classification: **PASS / LOCAL**.
- surviving sources: **F^9 + gamma_2^3 only**.
- gamma_3^3 / gamma_4 secondary contribution: **PASS / LOCAL = zero**.
- independent new gamma_2^3 functional: **FAIL / CLOSED**.
- intrinsic function-valued delta_3 family: **PASS / CLOSED**.
- canonical filtered t_2 extraction: **OPEN / LOAD-BEARING**.
- deeper-than-P_4 factorization as a coordinate-free theorem: **OPEN / LOAD-BEARING**.
- HA61-B overall: **OPEN / LOAD-BEARING**.
- HA61-C: **not opened**.

## Immediate consequence

The remaining B5 problem is no longer “what higher D_4 terms might exist?” The finite source audit has exhausted that list. The last door is now precise:

> Does the surviving F^9 functional, after the exact prefix/suffix cancellation and the old gamma_2^3 correction are retained together, descend to a natural filtered datum t_2 (or prove that no such datum exists)?

A proof closes B5 toward C. A counterexample closes the proposed t_2 factorization negatively. No intermediate “looks plausible” conclusion is permitted.

