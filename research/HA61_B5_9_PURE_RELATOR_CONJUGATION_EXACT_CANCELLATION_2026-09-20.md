# HA61-B5-9 — PURE RELATOR CONJUGATION: EXACT PREFIX/SUFFIX CANCELLATION — 2026-09-20

## Verdict

PASS / LOCAL for the exact crossed-cocycle invariance identity. This does not yet identify the filtered t_2 coordinate.

Let z be a crossed 1-cocycle for rho_3 and let r be the relator. Since rho_3(r)=1 and z(r)=0 for a cocycle on the presented group, for r' = v r v^{-1} one has exactly

  z(r') = z(v) + rho_3(v) z(r) + rho_3(vr) z(v^{-1}) = 0.

Here z(v^{-1}) = -rho_3(v)^{-1} z(v) and rho_3(vr)=rho_3(v). Thus pure relator conjugation cannot change the intrinsic obstruction, at any filtration order.

## Consequence for the B5 coordinate decomposition

The previously isolated degree-three relation-jet term [v,R] has secondary evaluation

  T([v,R]) = -lambda(v)(lambda wedge f)(R) = lambda(v) f(p)

on the primary-zero locus, so the raw t_2 coordinate shifts by lambda(v)p if this term is isolated.

The exact identity above proves that this isolated term cannot be the complete conjugation contribution. Since rho_3 and the intrinsic mu parameter are fixed under pure relator conjugation, the compensating term cannot be supplied by an intrinsic change mu -> mu + lambda(v).

Therefore the missing cancellation must occur among the other representative-level terms generated by expanding v r v^{-1}: in particular, the prefix/suffix crossed-cocycle contributions and any terms that were separated from the [v,R] jet contribution in the source ledger must be retained together.

This is stronger than the previous statement 'a missing source may exist': the full crossed-word identity forces the total sum of all conjugation-induced secondary pieces to be zero.

## Independent source check

The cancellation is algebraic and does not use q, chi, a chosen orientation, or the affine quotient. It is therefore an independent verification of presentation invariance of the full relator evaluation.

It also rules out the shortcut that the [v,R] term itself defines the gauge action on t_2. That term is only one component of a larger exact cancellation.

## Boundary

Closed:
- exact invariance of the full crossed-cocycle evaluation under r -> v r v^{-1};
- pure presentation conjugation does not change rho_3 or mu;
- the isolated [v,R] contribution is not the complete gauge contribution.

Open:
- explicit degree-three decomposition of the prefix/suffix cancellation;
- whether the remaining sum reduces exactly to a coordinate change of t_2;
- P_4 residual -> intrinsic filtered datum;
- deeper-than-P_4 factorization.

HA61-B remains OPEN / LOAD-BEARING. HA61-C remains unopened.

## Next authorized calculation

Expand z(v r v^{-1}) through the same mod-27 / divided-by-9 filtration used for t_2, without replacing the exact expression by the relation-jet transformation P -> P+[v,R]. Keep the prefix term z(v), the inverse-prefix term, the rho_3(v) factors, and the [v,R] contribution in one ledger. The target is to identify the exact cancellation partner of lambda(v)f(p).